apiVersion: apps/v1
kind: Deployment
metadata:
  name: phototeditor-brightnessContrast-depl
spec:
  selector:
    matchLabels:
      app: photoeditor-brightnessContrast-app
  replicas: 1
  template:
    metadata:
      labels:
        app: photoeditor-brightnessContrast-app
    spec:
      containers:
      - name: brightnessContrast
        image: saad12ah/brightnessContrast
        resources:
          limits:
            cpu: "0.5" # Limit the pod to use a maximum of 0.5 CPU cores
            memory: "1000Mi" # Limit the pod to use a maximum of 512 megabytes of memory
          requests:
            cpu: "0.2" # Request at least 0.2 CPU cores for the pod
            memory: "1000Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: brightnessContrast-servc
spec: 
  type: NodePort
  selector: 
    app: photoeditor-brightnessContrast-app
  ports:
  - name: my-app
    port: 8016
    nodePort: 30016
    targetPort: 8016

